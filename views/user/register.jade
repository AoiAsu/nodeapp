extends ../layout

block content
    h1 会員登録

    if flash.error
        hr
        b= flash.error
        hr

    if isSuccess
        会員登録に成功しました。
        p ID: #{user.userId}
        a(href='/') [ログイン]
    else
        p IDとパスワードを入力してください。
        form(action='/user/register', method='post')
            fieldset
                legend 会員登録
                p
                    label(for='user[userId]') ユーザID
                    input(name='user[userId]')
                p
                    label(for='user[pwd]') パスワード
                    input(type='password', name='user[pwd]')
                input(type='submit')