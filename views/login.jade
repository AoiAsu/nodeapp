if flash.error
    hr
    b= flash.error
    hr
if user
    form(method='post')
        input(name='_method', type='hidden', value='DELETE')
        p #{user.userId}さん、こんにちは!
        a(href='javascript:', onClick='forms[0].submit()') [ログアウト]

else
    p ログインしてください。
    form(method='post')
        fieldset
            legend ログイン
            p
                label(for='user[userId]') ユーザ名
                input(name='user[userId]')
            p
                label(for='user[pwd]') パスワード
                input(type='password', name='user[pwd]')
            input(type='submit')